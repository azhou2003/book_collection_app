<h1>Confirm Deletion</h1>

<p>Are you sure you want to delete "<%= @book.title %>"?</p>

<%= form_with url: book_path(@book), method: :delete, local: true do %>
  <%= submit_tag 'Confirm'%>
  <%= link_to 'Back to books', books_path%>
<% end %>
